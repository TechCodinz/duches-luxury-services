"use client";
import { useState } from "react";
import { Crown, LockKeyhole } from "lucide-react";

export default function AdminLogin(){const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const form=new FormData(e.currentTarget);const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),accessCode:form.get("accessCode")})});const data=await response.json();if(response.ok){window.location.href="/admin"}else{setError(data.error||"Unable to sign in");setLoading(false)}}return <main className="login-page"><section className="login-card"><div className="brand-mark"><Crown size={22}/></div><p className="eyebrow">Duches Private Office</p><h1>Owner & staff access</h1><p>Secure access to listings, guest operations, finance, media and social commerce.</p><form onSubmit={submit}><label>Email<input name="email" type="email" required placeholder="you@duches..."/></label><label>Access code<input name="accessCode" type="password" required placeholder="••••••••"/></label>{error&&<div className="login-error">{error}</div>}<button disabled={loading}><LockKeyhole size={16}/>{loading?"Signing in…":"Enter command centre"}</button></form><small>Production onboarding will replace the temporary access-code flow with named staff accounts and role-based authentication.</small></section></main>}
