"use client";

import Link from "next/link";
import { useEffect, useMemo, useRef, useState } from "react";
import { upload } from "@vercel/blob/client";
import { ArrowLeft, FileAudio, FileVideo2, ImageIcon, Loader2, Search, Trash2, UploadCloud } from "lucide-react";
import { demoAssets, type MediaKind } from "../../../lib/cms";

type Asset={id:string;kind:MediaKind;name?:string;url:string;mime_type?:string;size_bytes?:number;created_at?:string;duration?:string;size?:string;createdAt?:string};
const icons:Record<MediaKind,React.ReactNode>={image:<ImageIcon size={17}/>,video:<FileVideo2 size={17}/>,audio:<FileAudio size={17}/>};
export default function MediaLibrary(){const [kind,setKind]=useState<"all"|MediaKind>("all");const [assets,setAssets]=useState<Asset[]>(demoAssets as unknown as Asset[]);const [query,setQuery]=useState("");const [uploading,setUploading]=useState(false);const [notice,setNotice]=useState("");const picker=useRef<HTMLInputElement>(null);useEffect(()=>{refresh()},[]);async function refresh(){try{const r=await fetch("/api/cms/media");const d=await r.json();if(d?.items)setAssets(d.items)}catch{}}async function onFiles(files:FileList|null){if(!files?.length)return;setUploading(true);setNotice("");try{for(const file of Array.from(files)){await upload(`duches/${Date.now()}-${file.name}`,file,{access:"public",handleUploadUrl:"/api/media/upload",clientPayload:JSON.stringify({originalName:file.name})})}setNotice("Upload complete. Your media is now available to Duches.");await refresh()}catch(error){setNotice(error instanceof Error?error.message:"Upload failed. Storage may not be connected yet.")}finally{setUploading(false);if(picker.current)picker.current.value=""}}async function remove(id:string){const r=await fetch(`/api/cms/media/${id}`,{method:"DELETE"});if(r.ok)setAssets(items=>items.filter(x=>x.id!==id))}const filtered=useMemo(()=>assets.filter(a=>(kind==="all"||a.kind===kind)&&`${a.name??""} ${a.url}`.toLowerCase().includes(query.toLowerCase())),[assets,kind,query]);return <main className="cms-page"><section className="cms-hero"><div><Link className="ops-back" href="/admin/listings"><ArrowLeft size={15}/> Listings</Link><p className="eyebrow">Duches Media</p><h1>Upload the stories that sell the experience.</h1><p>Photos, walkthrough videos, reels, ambience audio and campaign assets can now flow through the production media route and into the reusable library.</p></div><label className="cms-primary upload-button">{uploading?<Loader2 size={16}/>:<UploadCloud size={16}/>} {uploading?"Uploading…":"Upload media"}<input ref={picker} type="file" multiple accept="image/*,video/*,audio/*" disabled={uploading} onChange={e=>onFiles(e.target.files)}/></label></section><section className="cms-toolbar"><div className="cms-search"><Search size={16}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search media"/></div><div className="media-filters">{(["all","image","video","audio"] as const).map(x=><button key={x} className={kind===x?"active":""} onClick={()=>setKind(x)}>{x}</button>)}</div></section>{notice&&<div className="storage-note"><UploadCloud size={21}/><div><strong>Media status</strong><p>{notice}</p></div></div>}<section className="media-grid">{filtered.map(asset=><article className="media-card" key={asset.id}><div className={`media-preview ${asset.kind}`} style={asset.kind!=="audio"?{backgroundImage:`url(${asset.url})`}:undefined}>{asset.kind==="audio"&&<FileAudio size={34}/>}<span>{icons[asset.kind]} {asset.kind}</span></div><div className="media-meta"><div><strong>{asset.name??asset.url.split("/").pop()}</strong><small>{asset.duration??asset.size??(asset.size_bytes?`${Math.round(asset.size_bytes/1024)} KB`:"")} · {asset.created_at?new Date(asset.created_at).toLocaleDateString():asset.createdAt??"Today"}</small></div><button onClick={()=>remove(asset.id)} aria-label={`Delete ${asset.name??"media"}`}><Trash2 size={16}/></button></div></article>)}</section><section className="storage-note"><UploadCloud size={21}/><div><strong>Production media path</strong><p>When BLOB_READ_WRITE_TOKEN is present, files upload directly from the browser to Vercel Blob and metadata is recorded in Postgres after completion.</p></div></section></main>}
