"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { ArrowLeft, CheckCircle2, ExternalLink, RefreshCw, XCircle } from "lucide-react";
type Transfer={id:string;client_name:string;sender_bank:string;sender_account_name:string;transfer_reference:string;amount:number;currency:string;proof_url?:string;status:string;created_at:string;invoice_reference:string};
export default function PendingTransfers(){const[items,setItems]=useState<Transfer[]>([]);const[notice,setNotice]=useState("");async function load(){const r=await fetch('/api/admin/bank-transfer');const d=await r.json();if(r.ok)setItems(d.items||[])}useEffect(()=>{load()},[]);async function review(id:string,action:'confirm'|'reject'){setNotice('Updating…');const r=await fetch(`/api/admin/bank-transfer/${id}/review`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action})});const d=await r.json();setNotice(r.ok?`Transfer ${d.status}.`:d.error||'Unable to review transfer.');if(r.ok)load()}return <main className="ops-page"><section className="ops-hero"><div><Link className="ops-back" href="/admin/payments"><ArrowLeft size={15}/> Finance</Link><p className="eyebrow">Manual settlement</p><h1>Pending bank transfers</h1><p>Review the actual bank receipt before confirming. A client-uploaded receipt is evidence only—not proof that funds settled.</p></div><button className="ops-hero-badge" onClick={load}><RefreshCw size={17}/> Refresh queue</button></section><section className="ops-panel"><div className="ops-panel-head"><div><p className="eyebrow dark-eye">Verification queue</p><h2>{items.length} transfer{items.length===1?'':'s'} awaiting review</h2></div></div>{items.length===0?<div className="empty-state">No pending transfers.</div>:items.map(x=><article className="transfer-review-row" key={x.id}><div><small>{x.invoice_reference}</small><strong>{x.client_name}</strong><p>{x.sender_bank} · {x.sender_account_name}</p><small>{x.transfer_reference||'No reference supplied'}</small></div><strong>{x.currency} {Number(x.amount).toLocaleString()}</strong><span>{new Date(x.created_at).toLocaleString()}</span>{x.proof_url?<a href={x.proof_url} target="_blank" rel="noreferrer"><ExternalLink size={15}/> View proof</a>:<span>No proof</span>}<div className="transfer-actions"><button onClick={()=>review(x.id,'confirm')}><CheckCircle2 size={15}/> Confirm</button><button className="danger" onClick={()=>review(x.id,'reject')}><XCircle size={15}/> Reject</button></div></article>)}</section>{notice&&<p className="cms-notice">{notice}</p>}</main>}
