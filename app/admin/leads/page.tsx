import Link from "next/link";
import { ArrowLeft, ArrowUpRight, Filter, Sparkles } from "lucide-react";
import { leads } from "../../../lib/operations";

export default function LeadsPage(){return <main className="ops-page"><section className="ops-hero"><div><Link className="ops-back" href="/admin"><ArrowLeft size={15}/> Command centre</Link><p className="eyebrow">Revenue intelligence</p><h1>Every inquiry becomes a trackable opportunity.</h1><p>Score intent, preserve the source conversation, assign ownership and move qualified guests toward an approved proposal, payment or private viewing.</p></div><div className="ops-hero-badge"><Sparkles size={18}/> AI lead scoring</div></section><section className="ops-kpis"><article><strong>18</strong><span>New inquiries</span></article><article><strong>7</strong><span>Qualified today</span></article><article><strong>$28.3K</strong><span>Hospitality pipeline</span></article><article><strong>94%</strong><span>VIP response SLA</span></article></section><section className="ops-panel"><div className="ops-panel-head"><div><p className="eyebrow dark-eye">Live pipeline</p><h2>Priority leads</h2></div><Filter size={20}/></div><div className="lead-table">{leads.map(lead=><article className="lead-row" key={lead.id}><div><small>{lead.id} · {lead.source}</small><strong>{lead.name}</strong><p>{lead.request}</p></div><span className="lead-score">{lead.score}<small>score</small></span><strong className="lead-value">{lead.value}</strong><span className={`status-pill ${lead.stage}`}>{lead.stage}</span><button className="icon-action" aria-label={`Open ${lead.name}`}><ArrowUpRight size={17}/></button></article>)}</div></section></main>}
