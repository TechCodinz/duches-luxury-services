import Link from"next/link";import{ArrowLeft,Quote,Star}from"lucide-react";import{reviews}from"../../../lib/business-suite";
export default function Reviews(){return <main className="ops-page"><section className="ops-hero"><div><Link className="ops-back" href="/admin"><ArrowLeft size={15}/> Command centre</Link><p className="eyebrow">Reputation</p><h1>Turn exceptional service into visible trust.</h1><p>Collect post-stay reviews, moderate them, feature approved testimonials on the website and reuse strong client stories in campaigns.</p></div><div className="ops-hero-badge"><Star size={18}/> 5.0 reputation</div></section><section className="ops-panel"><div className="ops-panel-head"><div><p className="eyebrow dark-eye">Review inbox</p><h2>Client voices</h2></div><Quote size={20}/></div>{reviews.map(r=><article className="review-row" key={r.id}><div className="review-stars">{Array.from({length:r.rating}).map((_,i)=><Star key={i} size={13} fill="currentColor"/>)}</div><div><strong>{r.name}</strong><small>{r.source}</small><p>“{r.copy}”</p></div><span className="status-pill">{r.status}</span><button className="cms-secondary">{r.status==="Pending"?"Approve":"Feature"}</button></article>)}</section></main>}