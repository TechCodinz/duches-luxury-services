import Link from "next/link";
import { ArrowLeft, Bot, CheckCircle2, ShieldCheck, Zap } from "lucide-react";
import { automations } from "../../../lib/operations";

export default function AutomationsPage(){return <main className="ops-page"><section className="ops-hero"><div><Link className="ops-back" href="/admin"><ArrowLeft size={15}/> Command centre</Link><p className="eyebrow">Automation control</p><h1>Always responsive. Never reckless.</h1><p>Duches automation handles repetitive coordination while price changes, refunds, inventory promises and sensitive VIP decisions remain approval-gated.</p></div><div className="ops-hero-badge"><ShieldCheck size={18}/> Human-controlled AI</div></section><section className="ops-grid two"><div className="ops-panel"><div className="ops-panel-head"><div><p className="eyebrow dark-eye">Live workflows</p><h2>Automation library</h2></div><Zap size={20}/></div>{automations.map(item=><article className="automation-rule" key={item.name}><span className="rule-icon"><Bot size={18}/></span><div><strong>{item.name}</strong><p>{item.action}</p></div><span className="status-pill confirmed">{item.state}</span></article>)}</div><div className="ops-panel"><div className="ops-panel-head"><div><p className="eyebrow dark-eye">Guardrails</p><h2>Approval centre</h2></div><ShieldCheck size={20}/></div><div className="approval-card"><small>PAYMENT REQUEST · INSTAGRAM</small><h3>Royal Indulgence deposit</h3><p>AI prepared a $2,500 deposit request after the guest selected Aug 21—24. Availability must be confirmed by staff before sending.</p><div className="approval-actions"><button>Approve & send</button><button className="secondary">Review conversation</button></div></div><div className="safety-list"><p><CheckCircle2 size={15}/> Never invent property availability</p><p><CheckCircle2 size={15}/> Never change approved prices</p><p><CheckCircle2 size={15}/> Never issue refunds without authorization</p><p><CheckCircle2 size={15}/> Escalate legal, safety and sensitive requests</p></div></div></section></main>}
