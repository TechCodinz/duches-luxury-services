import Link from"next/link";import{ArrowLeft,ShieldCheck,UserPlus,Users}from"lucide-react";import{team}from"../../../lib/business-suite";
export default function Team(){return <main className="ops-page"><section className="ops-hero"><div><Link className="ops-back" href="/admin"><ArrowLeft size={15}/> Command centre</Link><p className="eyebrow">Team & permissions</p><h1>Give every person exactly the access they need.</h1><p>Separate content, guest operations and finance responsibilities while preserving owner-level approval over sensitive actions.</p></div><button className="cms-primary"><UserPlus size={15}/> Invite team member</button></section><section className="ops-panel"><div className="ops-panel-head"><div><p className="eyebrow dark-eye">Access control</p><h2>Duches team</h2></div><ShieldCheck size={20}/></div>{team.map(t=><article className="team-row" key={t.name}><span className="team-avatar"><Users size={17}/></span><div><strong>{t.name}</strong><p>{t.role}</p></div><span>{t.access}</span><span className="status-pill">{t.state}</span></article>)}</section></main>}