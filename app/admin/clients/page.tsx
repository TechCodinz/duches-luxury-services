import Link from "next/link";import{ArrowLeft,Crown,MessageCircle,Search,Star}from"lucide-react";import{clients}from"../../../lib/business-suite";
export default function Clients(){return <main className="ops-page"><section className="ops-hero"><div><Link className="ops-back" href="/admin"><ArrowLeft size={15}/> Command centre</Link><p className="eyebrow">Private client CRM</p><h1>Remember the person, not just the booking.</h1><p>Keep preferences, lifetime value, previous stays, source channels and next-best actions together so every returning guest receives a more personal Duches experience.</p></div><div className="ops-hero-badge"><Crown size={18}/> Relationship intelligence</div></section><section className="ops-panel"><div className="ops-panel-head"><div><p className="eyebrow dark-eye">Client book</p><h2>Private relationships</h2></div><div className="cms-search"><Search size={15}/><input placeholder="Search clients"/></div></div><div className="client-cards">{clients.map(c=><article className="client-card" key={c.id}><div className="client-avatar">{c.name.split(" ").map(x=>x[0]).join("")}</div><div className="client-main"><small>{c.id} · {c.location}</small><h3>{c.name}{c.vip&&<Star size={14} fill="currentColor"/>}</h3><p>{c.tier}</p><div className="client-facts"><span><MessageCircle size={13}/>{c.channel}</span><span>Lifetime: {c.lifetime}</span></div></div><div className="client-next"><small>Next best action</small><strong>{c.next}</strong><span>Last: {c.last}</span></div></article>)}</div></section></main>}